{
  "buildCommand": "npm run build -w packages/shared && npm run build -w apps/web",
  "installCommand": "npm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/execute", "schedule": "0 0 * * *" }
  ]
}
